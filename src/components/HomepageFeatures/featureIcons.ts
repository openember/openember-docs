/** 图片放在 `static/img/`，路径为站点根下的 `/img/...`（Docusaurus 静态资源）。 */
export const homepageFeatureIconPath = {
  distributed: '/img/distributed-icon.png',
  middleware: '/img/middleware-icon.png',
  buildTool: '/img/build-tool-icon.png',
} as const;
